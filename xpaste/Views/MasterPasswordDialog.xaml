<Window x:Class="xpaste.Views.MasterPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="xpaste — Unlock" Height="320" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBody}"
        md:ThemeAssist.Theme="Inherit">
    <Grid Margin="40,32,40,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="xpaste" FontSize="30" FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" x:Name="SubtitleText" Text="Enter master password"
                   HorizontalAlignment="Center" Margin="0,6,0,0"
                   Foreground="{DynamicResource MaterialDesignBody}"
                   Opacity="0.7"/>

        <!-- spacer row 2 -->

        <PasswordBox Grid.Row="3" x:Name="PasswordBox"
                     md:HintAssist.Hint="Master password"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                     KeyDown="PasswordBox_KeyDown"/>

        <TextBlock Grid.Row="4" x:Name="ErrorText" Text=""
                   Foreground="#EF5350"
                   HorizontalAlignment="Center" Margin="0,8,0,0"/>

        <!-- spacer row 5 -->

        <Button Grid.Row="6" Content="UNLOCK"
                HorizontalAlignment="Stretch"
                Style="{StaticResource MaterialDesignRaisedButton}"
                Click="OnUnlock"/>
    </Grid>
</Window>
